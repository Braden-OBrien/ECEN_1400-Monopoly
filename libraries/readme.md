These libraries must be extracted to Documents\Arduino\libraries to be usable by the Arduino IDE.

"aaaaaaaaa" is the LiquidCrystal_I2C library modified to work for LCD2004 LCDs. Also included is the original library for HD44780 LCDs in the case that those are used.
Only include one of these, as they contain duplicate types.
